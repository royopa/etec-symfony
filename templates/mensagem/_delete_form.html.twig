<form method="post" action="{{ path('mensagem_delete', {'id': mensagem.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mensagem.id) }}">
    <button class="btn">Delete</button>
</form>
