{% extends 'base.html.twig' %}

{% block title %}Edit Mensagem{% endblock %}

{% block body %}
    <h1>Edit Mensagem</h1>

    {{ include('mensagem/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('mensagem_index') }}">back to list</a>

    {{ include('mensagem/_delete_form.html.twig') }}
{% endblock %}
