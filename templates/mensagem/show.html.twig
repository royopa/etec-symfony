{% extends 'base.html.twig' %}

{% block title %}Mensagem{% endblock %}

{% block body %}
    <h1>Mensagem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mensagem.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ mensagem.createdAt ? mensagem.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>TopicName</th>
                <td>{{ mensagem.topicName }}</td>
            </tr>
            <tr>
                <th>TopicValue</th>
                <td>{{ mensagem.topicValue }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mensagem_index') }}">back to list</a>

    <a href="{{ path('mensagem_edit', {'id': mensagem.id}) }}">edit</a>

    {{ include('mensagem/_delete_form.html.twig') }}
{% endblock %}
